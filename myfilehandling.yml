---
- hosts: 172.31.6.107
  become: yes
  tasks:
  - include: myfile.yml
  - name: install tree on ubuntu
    apt:
      name: tree
      state: present

  - name: copy index file into node side
    copy:
      src: myindexpage
      dest: /var/www/html/index.html    